const defaultState ={
    images:[],
    selectedImage:[]
}

function reducer(state=defaultState,action){
    switch(action.type){
        default:
            return state
    }
}

export default reducer